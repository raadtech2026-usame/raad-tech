"""Ruptela protocol adapter — **not yet implemented.** Structural placeholder only, reserving
this vendor's place in the plugin-based `vendors/<name>/` layout (device-gateway multi-vendor
architecture). No Ruptela hardware has been procured and no vendor protocol documentation for it
exists anywhere in this repository — see `vendors/teltonika/__init__.py`'s identical reasoning and
the process it describes for actually implementing a new vendor adapter.
"""
