"""JT808 device-plane event shapes and the outbound publisher port (Phase 9.6). This package
holds only *outbound* event definitions this service publishes to the broker (`.claude/rules/
jt808.md` #1) — never anything consumed from or written to the Business API's database, and
never an import of `backend.raad` or any business module.
"""
