# MySQL 8.x configuration template — placeholder only.
# Real tuning (buffer pool sizing, partitioning support, replication settings for the
# primary + replica topology) to be set once capacity planning is complete.
# See docs/business/RAAD_Phase2_Enterprise_Architecture_v1_2.md §11.2, §13.
