# Prompts

Reserved for reusable prompt fragments referenced by agents/commands/skills (e.g. a shared
"read-the-docs-first" preamble, or a shared conflict-reporting format). None exist yet — populate
only when a prompt fragment is genuinely reused in more than one place, to avoid duplicating
guidance that already lives in `.claude/rules/`.

## Status
Placeholder only — no prompt fragments exist yet.
