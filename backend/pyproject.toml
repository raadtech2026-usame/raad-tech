# RAAD Business API — package configuration template.
#
# Placeholder only. Dependency management tool (Poetry / PDM / pip-tools), exact dependency
# versions, and tool configuration (pytest, ruff/black/mypy if adopted) are not yet decided by
# approved documentation. Do not fill this in without confirming tooling choices first.
#
# Known from docs/business/RAAD_Phase3.1_Backend_LLD_v1_2.md:
#   - FastAPI (async)
#   - SQLAlchemy (async ORM)
#   - Alembic (migrations)
#   - pydantic-settings (typed configuration)
#   - Argon2 or bcrypt (password hashing)
#   - arq or Celery on Redis (worker runtime — open item, unconfirmed)

[project]
name = "raad-business-api"
version = "0.0.0"
description = "RAAD Platform — Business API (FastAPI modular monolith)"
requires-python = ">=3.11"
