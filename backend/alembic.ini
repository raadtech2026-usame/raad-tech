# Alembic configuration template — placeholder only.
# Real script_location, sqlalchemy.url (from environment), and logging config
# to be filled in when migrations are authored (Phase 3.2 Database Design).

[alembic]
script_location = migrations
